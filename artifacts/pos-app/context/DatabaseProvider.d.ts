// Type stub for the platform-specific `DatabaseProvider.native.tsx` /
// `DatabaseProvider.web.tsx` pair. React Native's bundler resolves the
// platform extension at runtime, but TypeScript doesn't, so we point the
// type checker at the shape both implementations share.
export { DatabaseProvider } from "./DatabaseProvider.web";
